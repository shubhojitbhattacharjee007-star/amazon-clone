package com.amazonclone.repository;

/**
 * Repository layer for data access.
 * Concrete repositories will extend {@link org.springframework.data.jpa.repository.JpaRepository}.
 */
interface RepositoryPackage {
}
